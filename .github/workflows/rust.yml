name: Unit tests 

on:
  pull_request:
    branches: ["master"]

jobs:
  builds:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      - name: Tests 
        run: cargo tests --verbose
